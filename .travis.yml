language: perl
perl:
  - "5.10"
install:
  - cpanm -f Mail::SPF
  - cpanm Net::DNS
  - cpanm NetAddr::IP
  - cpanm -v --installdeps --notest .
notifications:
  email:
    on_success: change
    on_failure: always
    recipients:
        - andrew@topdog.za.net
